# 无人机侦查打击双层优化问题算例
图1和图2为两种场景，每个场景下有固定的场景参数设置，文件为CreateModel.m和InitEnv.m.

每种场景下有小、中、大三种规模的无人机和目标，每种规模下有六个不同的测试算例。

小规模算例中，目标个数取值为5,7，无人机数量取值为3,4,5.

中规模算例中，目标个数取值为15,17，无人机数量取值为7,8,9.

大规模算例中，目标个数取值为40,45，无人机数量取值为14,15,16.
